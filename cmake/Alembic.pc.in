prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${exec_prefix}/include

Name: Alembic
Description: Alembic C++ library for storing and sharing 3D geometry
Version: @PROJECT_VERSION@

Requires:
Requires.private:

Libs: -L${libdir} -lAlembic
Libs.private:

Cflags: -I${includedir}
Cflags.private:
